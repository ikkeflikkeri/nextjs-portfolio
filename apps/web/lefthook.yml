pre-commit:
  jobs:
    - run: npx ultracite fix --no-error-on-unmatched-pattern
      glob:
        - "**/*.js"
        - "**/*.jsx"
        - "**/*.ts"
        - "**/*.tsx"
        - "**/*.json"
        - "**/*.jsonc"
        - "**/*.css"
      stage_fixed: true
